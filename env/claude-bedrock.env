# Optional env overrides for claude-bedrock
# Put secrets in your AWS credentials file instead of here when possible.
# Example (preferred): aws configure --profile bedrock
# Then run: AWS_PROFILE=bedrock claude-bedrock

# If you really need env vars, uncomment and fill:
# export AWS_ACCESS_KEY_ID="..."
# export AWS_SECRET_ACCESS_KEY="..."
# export AWS_REGION="us-east-1"
# export ANTHROPIC_PROVIDER="bedrock"
